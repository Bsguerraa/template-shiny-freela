mod_p1_ui <- function(id) {
  ns <- NS(id)
  
  tagList(
    
  )
}

mod_p1_server <- function(id, filtered_data_p1) { #RECEBA SEUS DADOS AQUI
  moduleServer(id, function(input, output, session) {
    
  })
}